"""Physical AI Safety Agent web package.

Contains the FastAPI backend (``web.backend``) and a Next.js frontend
(``web/frontend``). The backend is a thin HTTP/SSE wrapper around the
existing ``gaitlab.*`` Python API; no business logic lives here.
"""
