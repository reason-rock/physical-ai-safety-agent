# Joint Limit Rules

- Standard unsupervised-hardware threshold: joint limit max ratio <= 0.85.
- Standard supported-test threshold: joint limit max ratio <= 0.95.
- Strict mode lowers these thresholds.
